source "https://rubygems.org"

# Test-only dependencies for this repo's own CI. Consumer projects import the
# Fastfile via import_from_git and use their own Gemfile — this is not loaded
# by them.
gem "rspec", "~> 3.13"
